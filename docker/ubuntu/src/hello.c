#include <stdio.h>

/*
HOWTO in Ubuntu: 
1) install gcc as 'apt-get install --no-install-recommends gcc libc6-dev'
2) compile it as 'gcc hello.c -o hello'
3) run it as './hello'
*/

int main() {
    puts("Hello there from Ubuntu!");
    return 0;
}
